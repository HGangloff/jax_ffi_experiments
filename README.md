Two examples of usage for JAX FFI interface:

- A well detailed C++ vanilla Gibbs sampler implementation called from JAX
- A `jnp.split` C++ reimplementation. Includes a use example for `RemainingRets`.
